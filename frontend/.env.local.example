NEXT_PUBLIC_API_BASE_URL=/api/v1
BACKEND_API_ORIGIN=http://localhost:8000
# Server-only. Set this to the same value as backend APP_AUTH_TOKEN when
# backend token auth is enabled. Do not prefix it with NEXT_PUBLIC_.
BACKEND_API_TOKEN=
